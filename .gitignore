# Build output
bin/
dist/
node_modules/
node_modules
*.tsbuildinfo

# Stray top-level binary (built when running `go build` without -o)
/turboweb-mcp-by-ikari

# Test artifacts
extension/coverage/

# Opendoor-specific PDF disclosure form data — not infrastructure
current-mappings.json
custom-tools.json
disclosure_data_model_fields.json
field_mapping_plan.json
field_positions.json
live-mappings.json
nc_disclosure_training_transcript.txt

# Beads / Dolt files (added by bd init)
.dolt/
*.db
.beads-credential-key

# Agent / tooling scratch dirs
.omc/
.claude/
.DS_Store
